import type { Metadata } from "next"; import { AppProvider } from "./context"; import "./globals.css"; export const metadata: Metadata = { title: "StreamVault - IPTV Player", description: "Modern IPTV Web Player", }; export default function RootLayout({ children, }: Readonly<{ children: React.ReactNode; }>) { return ( <html lang="en" className="dark"> <body className="antialiased"> <AppProvider>{children}</AppProvider> </body> </html> ); }
